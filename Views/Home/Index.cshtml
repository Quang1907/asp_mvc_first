@{
    ViewData["Title"] = "aHome Page";
    // Url.Action()

    // Url.Route()
    // Url.Link()

}

<ul>
    <li>asp-area="Area"</li>
    <li>asp-action="Action"</li>
    <li>asp-controller="Product"</li>
    <li>asp-route="default"</li>
    <li>asp-route...="123"</li>
</ul>
<a asp-action="PlanetInfo" asp-controller="Planet" asp-route-id="2">First</a>
@{
    var routeData = new Dictionary<string, string>()
    {
        {"name", "QUANGCNTT"},
        {"it", "29"},
        {"clas", "jocker"},
    };
}
<a asp-action="PlanetInfo"
   asp-controller="Planet"
   asp-route-id="2"
   asp-route-abc="3">First</a>

<a asp-action="PlanetInfo"
   asp-controller="Planet"
   asp-all-route-data="@routeData">First</a>


<div class="text-center">
    <h1 class="display-4">XINs CHAO</h1>
    <partial name="_AlertMessage"></partial>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>

<p>
    @*@Url.RouteUrl("default", new {action="Index", controller="home"})*@
    @Url.RouteUrl("Mars")
</p>
<p>
    @Url.ActionLink()
</p>

<p>
    @Url.Action("privacy")
</p>

<p>
    @Url.Action("privacy", new {id = 1, name ="Quang"})
</p>

<p>
    @Url.Action("Jupiter", "Planet")
</p>

<p>
    @Url.Action("PlanetInfo", "Planet", new {id = 3})
</p>

<p>
    @Url.Action("Index", "Product", new { area = "admin"})
</p>